# Phases 1-2 run on pure stdlib. Phase 3 (multi-tenant platform) adds:
cryptography>=42      # Fernet credential vault
fastapi>=0.110        # admin + compliance API
uvicorn[standard]>=0.29
httpx                 # TestClient / API testing
# Phase 4 (mock paper-trading dashboard):
streamlit>=1.40       # dashboard UI
plotly>=5.20          # price chart + indicator overlays
# Real-data validation + LIVE trading (optional; paper/mock need none of these):
kiteconnect>=4        # Zerodha live orders/quotes/history (LIVE mode only)
